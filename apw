#!/bin/bash
i(){
  sudo apt update && sudo apt install $@
}

rm(){
  sudo apt update && sudo apt purge $@ && sudo apt autoremove -y
}

upgrade(){
  sudo apt update && sudo apt upgrade -y && sudo apt autoremove -y
}


"$@"
